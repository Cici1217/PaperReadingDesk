"""PaperReadingDesk backend modules.

The HTTP adapter remains in the project entrypoint for now, while persistence,
authentication, and resource rules live in this package so they can be tested
without rendering a page.
"""
